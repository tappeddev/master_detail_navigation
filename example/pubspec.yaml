name: master_detail_navigation_example
description: Example app for master_detail_navigation
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  auto_route: 10.1.2
  master_detail_navigation:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  auto_route_generator: ^10.2.5
  build_runner: ^2.10.5

flutter:
  uses-material-design: true
